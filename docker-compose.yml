services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: enterprise-user-management-api
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
    ports:
      - "8000:8000"
